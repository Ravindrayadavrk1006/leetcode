class Solution {
public:
    //since the player can start from either start or end and since the possibility of either one or two greater 
  is always there and told there can't be draw which state that alice always win
    bool stoneGame(vector<int>& piles) {
        return true;
    }
};
